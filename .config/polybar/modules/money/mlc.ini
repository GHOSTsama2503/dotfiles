[module/mlc]
type = custom/script

exec-if = which curl && which htmlq && which jq
exec = $XDG_CONFIG_HOME/polybar/scripts/money.sh MLC

interval = 3600

format = <label>

format-prefix = ""
format-prefix-font = 3
format-prefix-foreground = ${colors.green}

label = %output%
